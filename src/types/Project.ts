export interface Project {
  ProjectID: string;
  ProjectName: string;
  ObjectAddress: string;
}

export interface Account {
  UUID: string;
  AccountID: string;
  CustomerId: string;
  Category: string;
  Name: string;
}
